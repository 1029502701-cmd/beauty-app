const https = require("https"); const fs = require("fs"); const PROD = "https://ccfu.ccwu.cc"; const OUT = "C:/Users/yao/Documents/ChatGPT/美妆app/test_output"; async function apiPost(p, body, token) { return new Promise((res, rej) => { const hdrs = { "Content-Type": "application/json" }; if (token) hdrs["Authorization"] = "Bearer " + token; const r = https.request({ hostname: "ccfu.ccwu.cc", path: p, method: "POST", headers: hdrs }, x => { let d = ""; x.on("data", c => d += c); x.on("end", () => { try { res(JSON.parse(d)); } catch(e) { res({ raw: d.substring(0, 200) }); } }); }); r.on("error", rej); r.setTimeout(30000, () => { r.destroy(); rej(new Error("timeout")); }); r.write(JSON.stringify(body)); r.end(); }); } async function apiGet(p, token) { return new Promise((res, rej) => { const hdrs = {}; if (token) hdrs["Authorization"] = "Bearer " + token; const r = https.request({ hostname: "ccfu.ccwu.cc", path: p, method: "GET", headers: hdrs }, x => { let d = ""; x.on("data", c => d += c); x.on("end", () => { try { res(JSON.parse(d)); } catch(e) { res(d); } }); }); r.on("error", rej); r.setTimeout(30000, () => { r.destroy(); rej(new Error("timeout")); }); r.end(); }); } async function apiMultipartPost(p, fieldName, filePath, token) { return new Promise((res, rej) => { const bnd = "----E2EBoundary" + Date.now(); const buf = fs.readFileSync(filePath); const part1 = "--" + bnd + "\r\nContent-Disposition: form-data; name=\"" + fieldName + "\"; filename=\"photo.jpg\"\r\nContent-Type: image/jpeg\r\n\r\n"; const part2 = "\r\n--" + bnd + "--\r\n"; const body = Buffer.concat([Buffer.from(part1, "utf8"), buf, Buffer.from(part2, "utf8")]); const hdrs = { "Content-Type": "multipart/form-data; boundary=" + bnd }; if (token) hdrs["Authorization"] = "Bearer " + token; const r = https.request({ hostname: "ccfu.ccwu.cc", path: p, method: "POST", headers: hdrs }, x => { let d = ""; x.on("data", c => d += c); x.on("end", () => { try { res(JSON.parse(d)); } catch(e) { res(d); } }); }); r.on("error", rej); r.setTimeout(60000, () => { r.destroy(); rej(new Error("timeout")); }); r.end(body); }); } (async () => { console.log("=== Quick E2E Test on ccfu.ccwu.cc ===\n"); console.log("[1] Register..."); const reg = await apiPost("/api/auth/register", { account: "1" + String(Date.now()).slice(-10), password: "QuickTest1", confirmPassword: "QuickTest1" }); const token = reg.sessionId || reg.token; console.log("    Token:", token ? token.substring(0, 12) + "..." : "FAIL " + JSON.stringify(reg)); if (!token) process.exit(1); console.log("\n[2] Tier1 analyze..."); const t1 = await apiMultipartPost("/api/tier1/analyze", "photo", "C:/Users/yao/Documents/ChatGPT/美妆app/photo.jpg", token); console.log("    reportId:", t1.reportId || "FAIL " + JSON.stringify(t1).substring(0, 200)); if (!t1.reportId) process.exit(1); const tier1Id = t1.reportId; console.log("\n[3] Unlock by ad..."); const unlock = await apiPost("/api/tier2/unlock-by-ad", { tier1ReportId: tier1Id }, token); console.log("    tier2Id:", unlock.tier2ReportId || "FAIL " + JSON.stringify(unlock)); if (!unlock.tier2ReportId) process.exit(1); const tier2Id = unlock.tier2ReportId; console.log("\n[4] Poll tier2 status..."); let tier2Data = null; for (let i = 0; i < 60; i++) { await new Promise(r => setTimeout(r, 3000)); const st = await apiGet("/api/tier2/status?tier2Id=" + tier2Id, token); console.log("    poll " + (i+1) + ": " + st.generationStatus); if (st.generationStatus === "ready") { tier2Data = st.content; console.log("    READY!"); break; } if (st.generationStatus === "failed") { console.error("    FAILED!"); process.exit(1); } } if (!tier2Data) { console.error("    TIMEOUT!"); process.exit(1); } console.log("\n[5] Check product enrichment..."); const pr = tier2Data.productRecs || {}; let totalItems = 0, imgOk = 0, linkOk = 0; for (const dim of Object.keys(pr)) { for (const item of (pr[dim] || [])) { totalItems++; const hasImg = !!(item.imageUrl && item.imageUrl.length > 20); const hasLink = !!(item.itemUrl && item.itemUrl.length > 20); if (hasImg) imgOk++; if (hasLink) linkOk++; console.log("  [" + dim + "] " + item.name + " img=" + hasImg + " link=" + hasLink); } } console.log("\n  Total: " + totalItems + " | Images: " + imgOk + "/" + totalItems + " | Links: " + linkOk + "/" + totalItems); console.log("\n  " + (imgOk > 0 ? "SUCCESS!" : "STILL FAILING")); process.exit(0); })().catch(e => { console.error("FATAL:", e.message); process.exit(1); });
